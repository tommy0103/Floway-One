.claude/
.worktrees/
.wrangler
.eslintcache
node_modules/
docs/superpowers
.superpowers
apps/web/dist/
apps/web/.react-router/
*.tsbuildinfo
.dev.vars
.env
.pnpm-store/
/wrangler.jsonc

# Local node-target dev runtime (sqlite db + uploaded files). One under the
# repo root for `pnpm run dev:node` invoked at the root, one under the package
# itself when entry.ts resolves the default `./data/` relative to its own cwd.
/data/
apps/platform-node/data/

# Desktop bundles assemble the target-specific Node sidecar and production
# runtime immediately before Tauri builds the installer.
/apps/desktop/src-tauri/bundle-inputs/
/apps/desktop/src-tauri/bundle-inputs.previous/
/apps/desktop/src-tauri/.bundle-staging/
/apps/desktop/src-tauri/.desktop-verification/
/apps/desktop/src-tauri/gen/
/apps/desktop/src-tauri/target/
